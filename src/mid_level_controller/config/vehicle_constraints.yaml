# Vehicle constraint parameters for the non-holonomic constraints node
# These values are based on the Gazebo Ackermann steering vehicle model

nonholonomic_constraints_node:
  ros__parameters:
    # Vehicle geometry
    wheelbase: 0.22            # Distance between front and rear axles [m]
    track_width: 0.20          # Distance between left and right wheels [m]
    wheel_radius: 0.04         # Wheel radius [m]

    # Velocity constraints
    max_velocity: 2.0          # Maximum forward/reverse speed [m/s]
    max_velocity_rate: 1.0     # Maximum acceleration [m/s²]

    # Steering constraints
    max_steering_angle: 35.0   # Maximum steering angle [degrees]
    max_steering_rate: 45.0    # Maximum steering angular velocity [degrees/s]

    # Topics
    input_topic: /teleop/raw_cmd
    output_topic: /vehicle/cmd
    constraints_topic: /vehicle/constraints
    publish_rate: 20.0

    # Sign detection integration
    sign_command_topic: /sign/command
    stop_velocity: 0.0         # Velocity when STOP sign detected [m/s]
    slow_velocity: 0.3         # Velocity when SLOW_DOWN sign detected [m/s]
    turn_velocity: 0.4         # Velocity when TURN sign detected [m/s]
    turn_heading: 20.0         # Heading angle for TURN commands [degrees]
